// roles/navatec.ts — Web Developer · NavaTec GmbH, the panel's content (R91: one chunk per panel). CV-derived,
// verbatim from the former roles.ts entry; the index row (title, employer, dates, parts) stays in ../roles.ts.
import type { RoleContent } from '../roles'

export default {
  bullets: [
    'Responsible for a new SaaS product, from the backend to the frontend, and its deployment to the cloud',
    'C# (ASP.NET MVC) and PHP (Laravel) backends with MySQL, PostgreSQL and Elasticsearch; frontend with jQuery, Bootstrap and Vue 2',
    'First job in an international team',
  ],
  stack: ['csharp-dotnet', 'php-laravel', 'mysql', 'postgresql', 'elasticsearch', 'vue'],
} satisfies RoleContent
