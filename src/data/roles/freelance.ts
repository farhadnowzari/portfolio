// roles/freelance.ts — Full-Stack Developer · Freelancer, the panel's content (R91: one chunk per panel). CV-derived,
// verbatim from the former roles.ts entry; the index row (title, employer, dates, parts) stays in ../roles.ts.
import type { RoleContent } from '../roles'

export default {
  bullets: [
    'Client projects during university, a wide range of technologies at first, then web development as the main path',
  ],
  stack: [],
} satisfies RoleContent
